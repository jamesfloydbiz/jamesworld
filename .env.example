# Supabase (legacy — used by the old React app / edge functions, not the static site).
# These are the publishable/anon values only; real secrets live in Supabase env vars.
VITE_SUPABASE_PROJECT_ID=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_URL=
